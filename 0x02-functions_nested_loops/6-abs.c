#include "main.h"
/**
 * _abs - Calculates the value of the integer
 * @n: The integer to calculated.
 *
 * Return: The value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
